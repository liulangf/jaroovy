package com.liulangf.java8;


/**
 * <p>The problem with a completely new programming paradigm isn't learning a new 
 * language. 
 * 
 * <p>Language syntax is merely details.
 * 
 * <p>The tricky part is learning to think in a different way.
 * 
 */
public class FunctionalThinking_NealFord {

	//favor ideas over syntax
	void favor_ideas_over_syntax() {}
	
	/**
	 * Java eased our interaction with memory management(GC);
	 * Functional programming languages allow us to replace other core building
	 * blocks with higher-order abstractions.
	 */
	void java_fp() {}
	
	/**
	 * OO makes code understandable by encapsulating moving parts;
	 * 
	 * FP makes code understandable by minimizing moving parts. 
	 */
	void object_oriented_Functional_programming() {}
	
	/**
	 * FP languages prefer a few key data structures(such as List, Set and Map) with
	 * highly optimized operations on those data structures.
	 */
	void data_structure() {}
	
	/**
	 * FP describes programs as expressions and transformations, 
	 * modeling mathematical formulas, and tries to avoid mutable state.
	 * [filter, transform, convert]
	 * [filter, map, reduce]  scala
	 * [filter, map, collect] java8
	 */
	void functional_processing() {}
	
	void thinking_at_a_higher_level_of_abstraction() {}
	
	void focus_on_results_over_steps() {}
	
	void syntax_is_the_way_you_express_ideas_in_a_language() {}
	
	/**
	 * In FP, you have far fewer abstractions, but each one is generic;
	 * Because FP relies heavily on passed parameters and composition, you 
	 * have fewer rules to learn about the interactions among moving parts.
	 */
	void parameters_composition() { }
	
	
	class Cede_control_of_low_level_details {
		
		void iteration_to_higher_oder_function() {}
		
		/**
		 * Let the language manage state.
		 * 
		 * Closures are also an excellent example of deferred execution.
		 */
		void clsure() {}
		
		void currying_and_partial_application() {}
		
		/**
		 * Let languages manage states for you.
		 */
		void recursion() {}
		
		void stream_and_work_reordering() {}
		
		void laziness() {}
	}
	
	void few_datastructure_many_operations() {}
	
}

